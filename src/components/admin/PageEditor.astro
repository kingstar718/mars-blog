---
// 挂载脚本，本体在 pageEditor.ts。单独成组件的理由见 NoteEditor.astro。
---

<script>
  import { mountPageEditor } from "./pageEditor";

  const slot = document.querySelector<HTMLElement>("[data-page-editor-slot]");
  if (slot) mountPageEditor(slot.dataset.pageSlug ?? "about");
</script>
