---
// 只有一段挂载脚本，编辑器本体在 noteEditor.ts。
//
// 单独做成一个组件，是为了让脚本按需发送：编辑入口本身是静态 HTML，
// 编辑器本体只在登录态才被 import 下来。
---

<script>
  import { mountNoteEditor } from "./noteEditor";

  mountNoteEditor();
</script>
